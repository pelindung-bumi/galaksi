config:
  envoyGateway:
    provider:
      kubernetes:
        envoyService:
          type: NodePort

deployment:
  envoyGateway:
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
