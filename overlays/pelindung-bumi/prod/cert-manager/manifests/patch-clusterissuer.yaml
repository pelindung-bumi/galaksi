apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    email: admin@pelindungbumi.dev 
    solvers:
      - selector:
          dnsZones:
            - "pelindungbumi.dev"
        dns01:
          cloudflare:
            email: admin@pelindungbumi.dev
